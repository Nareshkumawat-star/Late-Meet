diff --git a/src/content.css b/src/content.css
index 5180efc..5a35b29 100644
--- a/src/content.css
+++ b/src/content.css
@@ -4,32 +4,6 @@
 /* --- THEME TOKENS --- */
 @import "./theme.css";
 
-:root {
-  --lm-space-xs: 4px;
-  --lm-space-xs2: 6px;
-  --lm-space-sm: 8px;
-  --lm-space-sm2: 10px;
-  --lm-space-md: 12px;
-  --lm-space-md2: 14px;
-  --lm-space-lg: 20px;
-  --lm-space-xl: 24px;
-
-  --lm-radius-xs: 4px;
-  --lm-radius-sm: 8px;
-  --lm-radius-base: 10px;
-  --lm-radius-md: 12px;
-  --lm-radius-lg: 16px;
-  --lm-radius-xl: 18px;
-  --lm-radius-2xl: 20px;
-
-  --lm-font-size-xs: 11px;
-  --lm-font-size-sm: 12px;
-  --lm-font-size-base: 13px;
-  --lm-font-size-md: 14px;
-  --lm-font-size-lg: 20px;
-  --lm-font-size-xl: 24px;
-}
-
 /* ——— Floating Dashboard Button ——— */
 #mc-float-btn {
   position: fixed;
@@ -38,7 +12,7 @@
   z-index: 999999;
   display: flex;
   align-items: center;
-  gap: var(--lm-space-sm);
+  gap: 8px;
   cursor: pointer;
   opacity: 0;
   transform: translateY(20px) scale(0.9);
@@ -64,7 +38,7 @@
   position: relative;
   width: 52px;
   height: 52px;
-  border-radius: var(--lm-radius-lg);
+  border-radius: 16px;
   background: var(--bg-app);
   border: 1px solid var(--border-sub);
   display: flex;
@@ -83,7 +57,7 @@
 .mc-float-pulse {
   position: absolute;
   inset: -3px;
-  border-radius: var(--lm-radius-lg);
+  border-radius: 18px;
   border: 1px solid var(--border-element);
   opacity: 0.5;
   animation: mc-pulse 2s ease-in-out infinite;
@@ -103,7 +77,7 @@
 
 .mc-float-icon {
   position: relative;
-  font-size: var(--lm-font-size-xl);
+  font-size: 24px;
   z-index: 1;
   display: flex;
   color: var(--text-main);
@@ -114,11 +88,11 @@
   backdrop-filter: blur(12px);
   -webkit-backdrop-filter: blur(12px);
   color: var(--text-main);
-  font-size: var(--lm-font-size-sm);
+  font-size: 12px;
   font-weight: 600;
   font-family: "Inter", "Segoe UI", sans-serif;
-  padding: var(--lm-space-xs2) var(--lm-space-md);
-  border-radius: var(--lm-radius-sm);
+  padding: 6px 12px;
+  border-radius: 8px;
   white-space: nowrap;
   opacity: 0;
   transform: translateX(8px);
@@ -138,7 +112,7 @@
   display: flex;
   align-items: flex-start;
   justify-content: flex-end;
-  padding: var(--lm-space-xl);
+  padding: 24px;
   opacity: 0;
   transform: translateX(100%);
   transition: all 0.5s cubic-bezier(0.16, 1, 0.3, 1);
@@ -157,11 +131,11 @@
   background: var(--bg-app);
   backdrop-filter: blur(20px);
   -webkit-backdrop-filter: blur(20px);
-  border-radius: var(--lm-radius-lg);
+  border-radius: 20px;
   border: 1px solid var(--border-sub);
   box-shadow: 0 25px 80px var(--shadow-main);
   color: var(--text-main);
-  padding: var(--lm-space-xl);
+  padding: 24px;
   font-family:
     "Inter",
     "Segoe UI",
@@ -175,26 +149,26 @@
 
 .mc-brief-card::-webkit-scrollbar-thumb {
   background: var(--border-element);
-  border-radius: var(--lm-radius-xs);
+  border-radius: 4px;
 }
 
 .mc-brief-header {
   display: flex;
   align-items: center;
-  gap: var(--lm-space-sm2);
-  margin-bottom: var(--lm-space-lg);
-  padding-bottom: var(--lm-space-md2); /* 14px - closest match, or add --lm-space-md3: 16px */
+  gap: 10px;
+  margin-bottom: 20px;
+  padding-bottom: 16px;
   border-bottom: 1px solid var(--border-main);
 }
 
 .mc-brief-icon {
-  font-size: var(--lm-font-size-xl);
+  font-size: 24px;
   color: var(--text-main);
 }
 
 .mc-brief-title {
   flex: 1;
-  font-size: var(--lm-font-size-md);
+  font-size: 14px;
   font-weight: 700;
   text-transform: uppercase;
   letter-spacing: 1px;
@@ -207,8 +181,8 @@
   color: var(--text-sub);
   width: 32px;
   height: 32px;
-  border-radius: var(--lm-radius-base);
-  font-size: var(--lm-font-size-md);
+  border-radius: 10px;
+  font-size: 14px;
   cursor: pointer;
   display: flex;
   align-items: center;
@@ -223,34 +197,34 @@
 }
 
 .mc-brief-greeting {
-  font-size: var(--lm-font-size-lg);
+  font-size: 20px;
   font-weight: 700;
-  margin-bottom: var(--lm-space-xs2);
+  margin-bottom: 6px;
   color: var(--text-main);
 }
 
 .mc-brief-text {
-  font-size: var(--lm-font-size-md);
+  font-size: 14px;
   color: var(--text-sub);
-  margin-bottom: var(--lm-space-lg);
+  margin-bottom: 20px;
   white-space: pre-wrap;
 }
 
 .mc-brief-section {
-  margin-bottom: var(--lm-space-md);
-  padding: var(--lm-space-md2);
+  margin-bottom: 16px;
+  padding: 14px;
   background: var(--bg-surface);
-  border-radius: var(--lm-radius-md);
+  border-radius: 12px;
   border: 1px solid var(--border-main);
 }
 
 .mc-brief-label {
-  font-size: var(--lm-font-size-sm);
+  font-size: 12px;
   font-weight: 700;
   text-transform: uppercase;
   letter-spacing: 0.5px;
   color: var(--text-main);
-  margin-bottom: var(--lm-space-sm);
+  margin-bottom: 8px;
   display: flex;
   align-items: center;
 }
@@ -264,7 +238,7 @@
 .mc-brief-list li {
   position: relative;
   padding: 6px 0 6px 18px;
-  font-size: var(--lm-font-size-base);
+  font-size: 13px;
   color: var(--text-sub);
   line-height: 1.5;
 }
@@ -278,21 +252,32 @@
 }
 
 .mc-brief-current {
-  font-size: var(--lm-font-size-md);
+  font-size: 14px;
   color: var(--text-main);
   font-weight: 500;
-  padding: var(--lm-space-sm) var(--lm-space-md);
+  padding: 8px 12px;
   background: var(--bg-card);
-  border-radius: var(--lm-radius-sm);
+  border-radius: 8px;
   border-left: 3px solid hsl(var(--accent-color));
 }
 
 .mc-brief-footer {
   text-align: center;
-  font-size: var(--lm-font-size-xs);
+  font-size: 11px;
   color: var(--text-muted);
-  margin-top: var(--lm-space-md2); /* 14px - closest match, or add --lm-space-md3: 16px */
-  padding-top: var(--lm-space-md);
+  margin-top: 16px;
+  padding-top: 12px;
   border-top: 1px solid var(--border-main);
   font-style: italic;
 }
+
+
+@media (prefers-reduced-motion: reduce) {
+  *,
+  *::before,
+  *::after {
+    animation: none !important;
+    transition: none !important;
+    scroll-behavior: auto !important;
+  }
+}
\ No newline at end of file
diff --git a/src/dashboard.css b/src/dashboard.css
index d3f4280..20620c2 100644
--- a/src/dashboard.css
+++ b/src/dashboard.css
@@ -4,7 +4,6 @@
 @import "https://fonts.googleapis.com/css2?family=Inter:wght=400;500;600;700;800;900&display=swap";
 
 /* --- THEME TOKENS --- */
-
 @import "./theme.css";
 
 * {
@@ -13,27 +12,16 @@
   box-sizing: border-box;
 }
 
-@media (prefers-reduced-motion: no-preference) {
-  html {
-    scroll-behavior: smooth;
-  }
-}
-
 body {
   font-family:
     "Inter",
     -apple-system,
     "Segoe UI",
     sans-serif;
-
   background: var(--bg-app);
   color: var(--text-main);
-
   min-height: 100vh;
   overflow-x: hidden;
-
-  scrollbar-width: thin;
-  scrollbar-color: var(--border-element) transparent;
 }
 
 body::-webkit-scrollbar,
@@ -41,68 +29,29 @@ body::-webkit-scrollbar,
 .mc-brief-card::-webkit-scrollbar {
   width: 5px;
 }
-
 body::-webkit-scrollbar-track,
-.transcript-list::-webkit-scrollbar-track,
-.mc-brief-card::-webkit-scrollbar-track {
+.transcript-list::-webkit-scrollbar-track {
   background: transparent;
 }
-
 body::-webkit-scrollbar-thumb,
-.transcript-list::-webkit-scrollbar-thumb,
-.mc-brief-card::-webkit-scrollbar-thumb {
+.transcript-list::-webkit-scrollbar-thumb {
   background: var(--border-element);
   border-radius: 5px;
 }
 
-body::before {
-  content: "";
-
-  position: fixed;
-  inset: 0;
-
-  background:
-    radial-gradient(circle at top left, rgba(99, 102, 241, 0.16), transparent 28%),
-    radial-gradient(circle at bottom right, rgba(168, 85, 247, 0.14), transparent 30%);
-
-  pointer-events: none;
-  z-index: -1;
-
-  animation: ambientMove 18s ease-in-out infinite alternate;
-}
-
-@keyframes ambientMove {
-  from {
-    transform: translateY(0) scale(1);
-  }
-
-  to {
-    transform: translateY(-20px) scale(1.04);
-  }
-}
-
 /* ——— Header ——— */
 .dash-header {
   position: sticky;
   top: 0;
   z-index: 100;
-
-  width: 100%;
-
   display: flex;
   align-items: center;
   justify-content: space-between;
-
   padding: 16px 20px;
-
-  background: rgba(10, 10, 10, 0.72);
-
-  backdrop-filter: blur(26px);
-  -webkit-backdrop-filter: blur(26px);
-
-  border-bottom: 1px solid rgba(255, 255, 255, 0.06);
-
-  box-shadow: 0 4px 24px rgba(0, 0, 0, 0.35);
+  background: var(--bg-app);
+  backdrop-filter: blur(20px);
+  -webkit-backdrop-filter: blur(20px);
+  border-bottom: 1px solid var(--border-main);
 }
 
 .dash-header-left {
@@ -249,72 +198,36 @@ body::before {
 }
 
 .dash-tab.active {
-  color: #ffffff;
-
-  background: linear-gradient(135deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.04));
-
-  border: 1px solid rgba(255, 255, 255, 0.08);
-
-  box-shadow:
-    0 8px 24px rgba(0, 0, 0, 0.35),
-    inset 0 1px 0 rgba(255, 255, 255, 0.05);
-
-  transform: translateY(-2px);
-} /* ——— Content ——— */
-.dash-content {
-  position: relative;
-
-  padding: 18px;
-  padding-bottom: 110px;
-
-  animation: contentFade 0.4s ease;
+  color: var(--text-main);
+  background: var(--bg-surface);
+  border: 1px solid var(--border-main);
 }
 
-/* Smooth page fade */
-@keyframes contentFade {
-  from {
-    opacity: 0;
-    transform: translateY(8px);
-  }
-
-  to {
-    opacity: 1;
-    transform: translateY(0);
-  }
+/* ——— Content ——— */
+.dash-content {
+  padding: 16px;
+  padding-bottom: 80px;
 }
 
 .tab-panel {
   display: none;
-
-  opacity: 0;
-  transform: translateY(10px) scale(0.99);
-
-  transition:
-    opacity 0.35s ease,
-    transform 0.35s ease;
+  animation: fadeSlide 0.3s ease;
 }
 
 .tab-panel.active {
   display: block;
+}
 
-  opacity: 1;
-  transform: translateY(0) scale(1);
 
-  animation: fadeSlide 0.35s cubic-bezier(0.4, 0, 0.2, 1);
-}
 
-/* Premium tab animation */
 @keyframes fadeSlide {
   from {
     opacity: 0;
-    transform: translateY(12px);
-    filter: blur(6px);
+    transform: translateY(4px);
   }
-
   to {
     opacity: 1;
     transform: translateY(0);
-    filter: blur(0);
   }
 }
 
@@ -322,61 +235,17 @@ body::before {
 .dash-card,
 .stat-card,
 .session-item {
-  position: relative;
-  overflow: hidden;
-
-  background: linear-gradient(145deg, rgba(255, 255, 255, 0.05), rgba(255, 255, 255, 0.02));
-
-  border: 1px solid rgba(255, 255, 255, 0.08);
-
-  backdrop-filter: blur(24px);
-  -webkit-backdrop-filter: blur(24px);
-
-  border-radius: 18px;
-
+  background: var(--bg-surface);
+  border: 1px solid var(--border-main);
+  border-radius: 12px;
   padding: 16px;
   margin-bottom: 16px;
-
-  box-shadow:
-    0 10px 35px rgba(0, 0, 0, 0.35),
-    inset 0 1px 0 rgba(255, 255, 255, 0.05);
-
-  transition:
-    transform 0.35s ease,
-    box-shadow 0.35s ease,
-    border-color 0.35s ease;
-}
-.dash-card::before,
-.stat-card::before,
-.session-item::before {
-  content: "";
-
-  position: absolute;
-  inset: -120%;
-
-  background: linear-gradient(120deg, transparent, rgba(255, 255, 255, 0.08), transparent);
-
-  transform: rotate(12deg);
-
-  transition: 0.8s;
-}
-
-.dash-card:hover::before,
-.stat-card:hover::before,
-.session-item:hover::before {
-  inset: 120%;
+  transition: all 0.2s cubic-bezier(0.4, 0, 0.2, 1);
 }
 
 .dash-card:hover,
-.session-item:hover,
-.stat-card:hover {
-  transform: translateY(-6px) scale(1.015);
-
-  border-color: rgba(255, 255, 255, 0.14);
-
-  box-shadow:
-    0 18px 45px rgba(0, 0, 0, 0.45),
-    0 0 0 1px rgba(255, 255, 255, 0.04);
+.session-item:hover {
+  border-color: var(--border-sub);
 }
 
 .glow-card {
@@ -419,19 +288,13 @@ body::before {
 }
 
 .live-dot {
-  width: 8px;
-  height: 8px;
-
+  width: 6px;
+  height: 6px;
   border-radius: 50%;
-
-  background: #22c55e;
-
-  box-shadow:
-    0 0 10px #22c55e,
-    0 0 20px rgba(34, 197, 94, 0.6);
-
-  animation: pulseLive 1.6s infinite;
+  background: var(--text-invert);
+  animation: blink 2s infinite;
 }
+
 @keyframes blink {
   0%,
   100% {
@@ -441,22 +304,6 @@ body::before {
     opacity: 0.5;
   }
 }
-@keyframes pulseLive {
-  0% {
-    transform: scale(1);
-    opacity: 1;
-  }
-
-  50% {
-    transform: scale(1.6);
-    opacity: 0.6;
-  }
-
-  100% {
-    transform: scale(1);
-    opacity: 1;
-  }
-}
 
 /* ——— Summary ——— */
 .dash-summary-text {
@@ -859,9 +706,6 @@ body::before {
   padding: 32px 20px;
   font-size: 13px;
   color: var(--text-muted);
-  border: 1px dashed var(--border-main);
-  border-radius: 10px;
-  background: var(--bg-app);
 }
 
 /* ——— Footer ——— */
@@ -895,13 +739,8 @@ body::before {
 }
 
 .export-btn:hover {
-  background: linear-gradient(135deg, rgba(255, 255, 255, 0.08), rgba(255, 255, 255, 0.03));
-
-  border-color: rgba(255, 255, 255, 0.12);
-
-  transform: translateY(-2px);
-
-  box-shadow: 0 10px 24px rgba(0, 0, 0, 0.35);
+  background: var(--bg-surface);
+  border-color: var(--border-element);
 }
 
 /* ——— Sessions List ——— */
@@ -1120,45 +959,6 @@ body::before {
   word-break: break-word;
 }
 
-.transcript-highlight {
-  background: rgba(255, 223, 107, 0.2);
-  box-shadow: inset 0 0 0 1px rgba(255, 223, 107, 0.6);
-  transition:
-    background 0.3s ease,
-    box-shadow 0.3s ease;
-}
-
-.timestamp-link {
-  margin-top: 6px;
-  display: inline-flex;
-  align-items: center;
-  gap: 4px;
-  padding: 3px 8px;
-  border: 1px solid var(--border-main);
-  border-radius: 999px;
-  background: var(--bg-surface);
-  color: var(--text-muted);
-  font-size: 11px;
-  cursor: pointer;
-  transition:
-    background 0.2s ease,
-    border-color 0.2s ease;
-}
-
-.timestamp-link:hover:not(:disabled) {
-  background: var(--bg-emphasis);
-  border-color: var(--border-emphasis);
-}
-
-.timestamp-link:disabled {
-  opacity: 0.5;
-  cursor: default;
-}
-
-.timestamp-text {
-  cursor: default;
-}
-
 /* Export wrapper */
 .export-wrapper {
   position: relative;
@@ -1238,390 +1038,15 @@ body::before {
 .export-toast.error {
   border-color: rgba(239, 68, 68, 0.4);
   color: #fca5a5;
-} /* ——— Premium Mobile Responsive ——— */
-@media (max-width: 768px) {
-  .stats-grid {
-    grid-template-columns: repeat(2, 1fr);
-    gap: 10px;
-  }
-
-  .dash-header {
-    flex-direction: column;
-    align-items: flex-start;
-    gap: 14px;
-
-    padding: 14px;
-
-    border-radius: 0 0 18px 18px;
-  }
-
-  .dash-header-right {
-    width: 100%;
-    justify-content: space-between;
-    gap: 10px;
-  }
-
-  .dash-content {
-    padding: 12px;
-    padding-bottom: 110px;
-  }
-
-  .dash-card,
-  .stat-card,
-  .session-item {
-    padding: 14px;
-
-    border-radius: 14px;
-
-    backdrop-filter: blur(18px);
-    -webkit-backdrop-filter: blur(18px);
-
-    box-shadow: 0 8px 24px rgba(0, 0, 0, 0.28);
-  }
-
-  .dash-card:hover,
-  .stat-card:hover {
-    transform: translateY(-2px);
-  }
-
-  .dash-timer {
-    font-size: 18px;
-  }
-
-  .dash-title {
-    font-size: 15px;
-  }
-
-  .dash-tab {
-    padding: 8px 14px;
-    font-size: 12px;
-  }
-
-  .dash-tab.active {
-    background: linear-gradient(135deg, rgba(255, 255, 255, 0.12), rgba(255, 255, 255, 0.04));
-
-    border: 1px solid rgba(255, 255, 255, 0.08);
-
-    box-shadow: 0 4px 18px rgba(0, 0, 0, 0.35);
-  }
-
-  .transcript-list {
-    max-height: 50vh;
-  }
-
-  .export-btn {
-    padding: 13px;
-    border-radius: 12px;
-
-    font-size: 13px;
-    font-weight: 600;
-  }
-
-  .live-indicator {
-    padding: 4px 9px;
-
-    border-radius: 999px;
-
-    box-shadow: 0 0 12px rgba(255, 255, 255, 0.08);
-  }
-
-  .action-item,
-  .decision-item,
-  .current-topic-box {
-    border-radius: 12px;
-  }
-}
-
-/* ——— Transcript Search ——— */
-.transcript-search-header {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: 12px;
-  margin-bottom: 12px;
-  padding: 10px;
-  background: var(--bg-app);
-  border: 1px solid var(--border-main);
-  border-radius: 10px;
-}
-
-.search-input-wrapper {
-  position: relative;
-  flex: 1;
-  min-width: 0;
-  display: flex;
-  align-items: center;
-}
-
-.search-icon {
-  position: absolute;
-  left: 10px;
-  color: var(--text-muted);
-  pointer-events: none;
-}
-
-.search-input {
-  width: 100%;
-  height: 36px;
-  background: var(--bg-surface);
-  border: 1px solid var(--border-sub);
-  border-radius: 8px;
-  padding: 8px 34px 8px 32px;
-  color: var(--text-main);
-  font-family: inherit;
-  font-size: 13px;
-  outline: none;
-  transition:
-    border-color 0.2s ease,
-    box-shadow 0.2s ease,
-    background 0.2s ease;
-}
-
-.search-input::placeholder {
-  color: var(--text-muted);
-}
-
-.search-input::-webkit-search-cancel-button {
-  -webkit-appearance: none;
-  appearance: none;
-  display: none;
-}
-
-.search-input:focus {
-  border-color: var(--border-element);
-  box-shadow: 0 0 0 3px var(--shadow-main);
 }
 
-.search-clear-btn {
-  position: absolute;
-  right: 8px;
-  width: 22px;
-  height: 22px;
-  background: transparent;
-  border: none;
-  border-radius: 6px;
-  color: var(--text-muted);
-  cursor: pointer;
-  display: none;
-  align-items: center;
-  justify-content: center;
-  padding: 2px;
-  transition:
-    background 0.2s ease,
-    color 0.2s ease,
-    opacity 0.2s ease;
-}
-
-.search-clear-btn.visible {
-  display: flex;
-}
-
-.search-clear-btn:hover:not(:disabled) {
-  color: var(--text-main);
-  background: var(--bg-card);
-}
 
-.search-clear-btn:disabled {
-  cursor: default;
-  opacity: 0.45;
-}
-
-.search-controls {
-  display: flex;
-  align-items: center;
-  gap: 8px;
-  flex-shrink: 0;
-}
-
-.search-counter {
-  min-width: 44px;
-  color: var(--text-muted);
-  font-size: 12px;
-  font-weight: 600;
-  text-align: center;
-  white-space: nowrap;
-  font-variant-numeric: tabular-nums;
-}
-
-.search-nav-buttons {
-  display: flex;
-  gap: 4px;
-}
-
-.search-action-btn {
-  width: 30px;
-  height: 30px;
-  background: var(--bg-surface);
-  border: 1px solid var(--border-sub);
-  border-radius: 7px;
-  padding: 4px;
-  color: var(--text-main);
-  cursor: pointer;
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  transition:
-    background 0.2s ease,
-    border-color 0.2s ease,
-    opacity 0.2s ease,
-    transform 0.2s ease;
-}
-
-.search-action-btn:hover:not(:disabled) {
-  background: var(--bg-card);
-  border-color: var(--border-element);
-  transform: translateY(-1px);
-}
-
-.search-action-btn:disabled {
-  opacity: 0.45;
-  cursor: default;
-}
-
-.search-match {
-  background: rgba(250, 204, 21, 0.5);
-  color: inherit;
-  border-radius: 3px;
-  padding: 0 2px;
-}
-
-.search-match.active {
-  background: #34d399;
-  color: #000000;
-  box-shadow: 0 0 0 1px rgba(52, 211, 153, 0.45);
-}
-
-@media (max-width: 520px) {
-  .transcript-search-header {
-    flex-wrap: wrap;
-  }
-
-  .search-input-wrapper {
-    flex-basis: 100%;
+@media (prefers-reduced-motion: reduce) {
+  *,
+  *::before,
+  *::after {
+    animation: none !important;
+    transition: none !important;
+    scroll-behavior: auto !important;
   }
-
-  .search-controls {
-    width: 100%;
-    justify-content: flex-end;
-  }
-}
-
-/* ——— History Modal & UI ——— */
-.session-item.expanded .session-item-summary {
-  display: block;
-  -webkit-line-clamp: unset;
-  line-clamp: unset;
-}
-
-.modal-overlay {
-  position: fixed;
-  top: 0;
-  left: 0;
-  width: 100vw;
-  height: 100vh;
-  background: rgba(0, 0, 0, 0.6);
-  backdrop-filter: blur(4px);
-  z-index: 1000;
-  display: flex;
-  align-items: center;
-  justify-content: center;
-  opacity: 1;
-  transition: opacity 0.2s ease;
-}
-
-.modal-overlay.hidden {
-  display: none;
-  opacity: 0;
-}
-
-.modal-content {
-  width: 90%;
-  max-width: 360px;
-  background: var(--bg-app);
-  margin-bottom: 0;
-  box-shadow: 0 10px 30px rgba(0, 0, 0, 0.8);
-}
-
-.modal-body {
-  font-size: 13px;
-  color: var(--text-sub);
-  line-height: 1.5;
-  margin-bottom: 24px;
-}
-
-.modal-actions {
-  display: flex;
-  justify-content: flex-end;
-  gap: 12px;
-}
-/* ——— Skeleton Loader ——— */
-@keyframes shimmer {
-  0% {
-    background-position: 200% 0;
-  }
-  100% {
-    background-position: -200% 0;
-  }
-}
-
-.skeleton-item {
-  background: linear-gradient(
-    90deg,
-    var(--bg-card) 25%,
-    var(--border-main) 50%,
-    var(--bg-card) 75%
-  );
-  background-size: 200% 100%;
-  animation: shimmer 1.5s infinite linear;
-  border-radius: 8px;
-  height: 40px;
-  margin-bottom: 8px;
-}
-
-.skeleton-avatar {
-  background: linear-gradient(
-    90deg,
-    var(--bg-card) 25%,
-    var(--border-main) 50%,
-    var(--bg-card) 75%
-  );
-  background-size: 200% 100%;
-  animation: shimmer 1.5s infinite linear;
-  border-radius: 8px;
-  height: 32px;
-  width: 32px;
-  flex-shrink: 0;
-}
-
-.skeleton-row {
-  display: flex;
-  align-items: center;
-  gap: 12px;
-  padding: 12px 0;
-  border-bottom: 1px solid var(--border-main);
-}
-.skeleton-row:last-child {
-  border-bottom: none;
-}
-.skeleton-text-block {
-  flex: 1;
-}
-.skeleton-text {
-  background: linear-gradient(
-    90deg,
-    var(--bg-card) 25%,
-    var(--border-main) 50%,
-    var(--bg-card) 75%
-  );
-  background-size: 200% 100%;
-  animation: shimmer 1.5s infinite linear;
-  border-radius: 4px;
-  height: 14px;
-  margin-bottom: 6px;
-  width: 80%;
-}
-.skeleton-text.short {
-  width: 40%;
-  height: 10px;
-  margin-bottom: 0;
-}
+}
\ No newline at end of file
diff --git a/src/popup.css b/src/popup.css
index 5b7cd00..d64ddda 100644
--- a/src/popup.css
+++ b/src/popup.css
@@ -270,6 +270,7 @@ body::-webkit-scrollbar-thumb {
   animation: pulse-dot 2s infinite;
 }
 
+
 @keyframes pulse-dot {
   0% {
     box-shadow: 0 0 0 0 var(--border-element);
@@ -303,11 +304,6 @@ body::-webkit-scrollbar-thumb {
   border-color: var(--border-main);
 }
 
-.icon-btn:focus-visible {
-  outline: 2px solid hsl(var(--accent-color));
-  outline-offset: 2px;
-}
-
 /* ——— Duration Bar ——— */
 .duration-bar {
   display: flex;
@@ -538,16 +534,6 @@ body::-webkit-scrollbar-thumb {
   background: var(--bg-card);
 }
 
-.topic-dot.unresolved {
-  background: #f59e0b;
-  box-shadow: 0 0 6px rgba(245, 158, 11, 0.4);
-}
-
-.topic-status.unresolved {
-  color: #fbbf24;
-  background: rgba(245, 158, 11, 0.15);
-}
-
 /* ——— Late Joiners ——— */
 .late-joiner-item {
   display: flex;
@@ -787,17 +773,6 @@ body::-webkit-scrollbar-thumb {
   line-height: 1.5;
 }
 
-.session-modal-error {
-  background: rgba(239, 68, 68, 0.12);
-  border: 1px solid rgba(239, 68, 68, 0.35);
-  border-radius: 8px;
-  color: #fecaca;
-  font-size: 12px;
-  line-height: 1.4;
-  margin: -8px 0 16px;
-  padding: 10px 12px;
-}
-
 .session-modal-actions {
   display: flex;
   gap: 10px;
@@ -837,3 +812,14 @@ body::-webkit-scrollbar-thumb {
   cursor: wait;
   pointer-events: none;
 }
+
+
+@media (prefers-reduced-motion: reduce) {
+  *,
+  *::before,
+  *::after {
+    animation: none !important;
+    transition: none !important;
+    scroll-behavior: auto !important;
+  }
+}
\ No newline at end of file
